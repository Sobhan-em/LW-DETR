name: Download LW-DETR Model

on:
  workflow_dispatch:

jobs:
  download-model:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Download model from Hugging Face
        run: |
          mkdir -p output
          wget https://huggingface.co/xbsu/LW-DETR/resolve/main/pretrain_weights/LWDETR_small_60e_coco.pth -O output/LWDETR_small_60e_coco.pth

      - name: Upload model as artifact
        uses: actions/upload-artifact@v3
        with:
          name: lwdetr-small-model
          path: output/LWDETR_small_60e_coco.pth